<div class="container p-4">
    <div class="row">
        <div class="col-md-4 mx-auto">
            <div class="card">
                <div class="card-body">
                    <form action="/fotos/edit/{{foto.id}}" method="POST">
                        <div class="form-group">
                            <input type="text" value="{{foto.titulo}}" class="form-control" name="titulo" placeholder="Nuevo titulo" autofocus>
                        </div>
                        <div class="form-group">
                            <input type="url" value="{{foto.url}}" class="form-control" name="url" placeholder="Nuevo url">
                        </div>
                        <div class="form-group">
                            <textarea class="form-control" name="descripcion" rows="2" placeholder="Nueva descripción">{{foto.descripcion}}</textarea>
                        </div>
                        <div class="form-group">
                            <button class="btn btn-success btn-block"><i class="fas fa-save"></i></button>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </div>

</div>